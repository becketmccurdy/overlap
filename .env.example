# Supabase Configuration
# Get these values from your Supabase project settings: https://app.supabase.com/project/_/settings/api

# Public Supabase URL (client-side, safe to expose)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Public Supabase Anon Key (client-side, safe to expose with RLS enabled)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (server-side only, NEVER expose to client)
# Used for API routes that bypass RLS policies
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Alternative naming (for compatibility with older code)
SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL}
SUPABASE_SERVICE_ROLE=${SUPABASE_SERVICE_ROLE_KEY}

# Next.js Environment
NODE_ENV=development
